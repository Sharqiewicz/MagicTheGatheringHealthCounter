import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  image: {
    maxWidth: '80%',
    height: 200,
    borderRadius: 25,
  },
  infoContainer: {
    position: 'absolute',
    width: '70%',
    height: 80,
    bottom: -30,
    left: '5%',
    backgroundColor: 'white',
    borderRadius: 25,
    shadowColor: '#171717',
    shadowRadius: 25,
    elevation: 10,
  },
})
