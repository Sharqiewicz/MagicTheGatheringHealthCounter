import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  textInput: {
    fontSize: 20,
    padding: 20,
    marginHorizontal: 20,
    borderRadius: 20,
    backgroundColor: '#dcdde1',
  },
  label: {
    fontSize: 12,
    marginTop: 10,
    marginBottom: 5,
    marginHorizontal: 10,
  },
})
